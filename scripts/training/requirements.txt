# =============================================================================
# SageMaker Training Dependencies
# =============================================================================
# This file is automatically detected and installed by SageMaker before training.
#
# IMPORTANT: SageMaker containers have pre-installed packages that may conflict.
# We use explicit versions with --upgrade behavior to override pre-installed versions.
#
# NOTE: SageMaker PyTorch containers already include:
#   - torch (version controlled by framework_version in launch script)
#   - numpy, scipy, pandas
#   - boto3, s3fs (for S3 access)
#   - tqdm, scikit-learn
# =============================================================================

# Hugging Face ecosystem - latest versions
transformers                 # Latest version
accelerate                   # Latest version
peft                         # Latest version
datasets                     # Latest version
tokenizers                   # Latest version
evaluate                     # Model evaluation metrics

# Monitoring
wandb                        # Experiment tracking

# Performance optimizations
flash-attn                   # Flash Attention 2 (may already be in SageMaker container)
bitsandbytes                 # For 8-bit/4-bit quantization (QLoRA)

# Tokenizer dependencies
sentencepiece                # Tokenization (required by some transformers)
protobuf>=3.20.0,<5.0.0      # Protocol buffers (required by sentencepiece)
