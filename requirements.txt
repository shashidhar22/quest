# Core data processing
pandas>=2.0.0
numpy>=1.24.0
dask[complete]>=2023.5.0

# Parquet support
pyarrow>=12.0.0
fastparquet>=2023.4.0

# YAML configuration
PyYAML>=6.0

# CSV parsing
python-snappy>=0.6.1

# JSON parsing (for iReceptor)
ijson>=3.2.0

# Parallel processing (built-in, but listing for reference)
# concurrent.futures - included in Python 3.2+

# Optional: Progress bars
tqdm>=4.65.0

# TCR sequence stitching (full-length reconstruction)
stitchr>=1.2.0

# Optional: Logging
# logging - included in Python standard library

# For database parsing
# scipy>=1.10.0  # If needed for any statistical operations

# Testing (optional)
# pytest>=7.3.0
# pytest-cov>=4.1.0
