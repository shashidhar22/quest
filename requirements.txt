# =============================================================================
# Quest - TCR/MHC Training and Fine-tuning Dependencies
# =============================================================================
# This file contains all dependencies for local development, training,
# fine-tuning, and supervised fine-tuning (SFT) of protein language models.
#
# For AWS Trainium/Inferentia instances, additional packages are pre-installed
# in the Neuron container (torch-xla, neuronx-distributed, etc.)
# =============================================================================

# Core data processing
pandas>=2.3.3
numpy>=2.3.4
dask[complete]>=2025.11.0

# Parquet support
pyarrow>=22.0.0
fastparquet>=2024.11.0

# YAML configuration
PyYAML>=6.0.3

# CSV parsing
python-snappy>=0.7.3

# JSON parsing (for iReceptor)
ijson>=3.4.0

# Fast JSON serialization (used by deduplicate_streaming.py, falls back to stdlib json)
orjson>=3.10.0

# Parallel processing
distributed>=2025.11.0
cloudpickle>=3.1.2
locket>=1.0.0
partd>=1.4.2
sortedcontainers>=2.4.0
tblib>=3.2.1
zict>=3.0.0

# Progress bars
tqdm>=4.67.1

# TCR sequence stitching (full-length reconstruction)
stitchr>=1.3.1
tidytcells>=2.2.0
# =============================================================================
# Deep Learning Frameworks
# =============================================================================
torch>=2.9.0
transformers>=4.57.1
tokenizers>=0.22.1
safetensors>=0.6.2
accelerate>=1.11.0

# Parameter-efficient fine-tuning (LoRA, QLoRA)
peft>=0.17.1
bitsandbytes>=0.43.0

# Flash Attention 2 (CUDA only - improves training speed)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# =============================================================================
# Hugging Face Ecosystem
# =============================================================================
huggingface-hub>=0.36.0
datasets>=4.4.1
evaluate>=0.4.6

# Tokenization
sentencepiece>=0.2.0
protobuf>=3.20.0,<5.0.0

# =============================================================================
# Training Infrastructure
# =============================================================================
# Contrastive learning
pytorch-metric-learning>=2.4.0

# Gradient scaling and mixed precision
# (included in torch, but ensure compatibility)

# Distributed training utilities
# torch.distributed is included in torch

# =============================================================================
# SageMaker Integration (optional, for launching remote jobs)
# =============================================================================
# sagemaker>=2.200.0
# boto3>=1.34.0

# =============================================================================
# Additional Utilities
# =============================================================================
fsspec>=2025.10.0
requests>=2.32.5
filelock>=3.20.0
regex>=2025.11.3
packaging>=25.0

# Compression
lz4>=4.4.5
cramjam>=2.11.0

# Monitoring and logging
wandb>=0.22.3
tensorboard>=2.15.0

# Machine learning utilities
scikit-learn>=1.6.0

# HDF5 support
h5py>=3.13.0

# Visualization
matplotlib>=3.10.0
seaborn>=0.13.0

# =============================================================================
# AWS Trainium/Neuron SDK (only on Trainium instances)
# =============================================================================
# These packages are pre-installed in the AWS Neuron container.
# Do NOT install on CUDA machines - they are XLA-specific.
#
# torch-neuronx>=2.1        # Neuron-optimized PyTorch
# neuronx-distributed       # Distributed training on Neuron
# neuronx-cc                # Neuron compiler
# torch-xla                 # XLA backend for PyTorch
# =============================================================================

# =============================================================================
# Testing (optional)
# =============================================================================
# pytest>=7.3.0
# pytest-cov>=4.1.0
